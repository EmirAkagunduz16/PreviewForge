{
  "name": "@previewforge/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "test": "pnpm test:unit",
    "test:unit": "vitest run src --exclude 'src/**/*.integration.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@previewforge/contracts": "workspace:*",
    "kafkajs": "2.2.4"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
